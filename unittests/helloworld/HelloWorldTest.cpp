/**
 * A test of the unit tests, if you will. This is a placeholder to demonstrate
 * that the GTEST unit testing framework is set up properly in this repo. This
 * can be removed after real tests are added.
 */
#include <gtest/gtest.h>

TEST(HelloWorld, assertTest) { ASSERT_TRUE(true); }